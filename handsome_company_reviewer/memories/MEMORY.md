Host: Windows 10. User home: C:\Users\Administrator. Shell is git-bash/MSYS (POSIX), NOT PowerShell — use bash syntax (ls, $HOME, &&, |, single quotes, MSYS-style /c/Users/... paths). Python: 3.11.9. Package manager: uv available, pip → python3.11. python3 command is MISSING — always invoke `python` not `python3`.
§
Hermes Agent v0.15.1 config v26, install at C:\Users\Administrator\AppData\Local\hermes\hermes-agent。Active profile 来自 system prompt(本会话=handsome_company_reviewer)。**Profile 布局陷阱**:profile 根 = ~/AppData/Local/hermes/profiles/<name>/,**home/ 子目录空**($HOME 指向它)。**状态全在 profile 根**:state.db(+WAL)/sessions/plans/workspace/memories/skills/cron/logs/.hermes_history/SOUL.md。诊断"上次任务"看 .hermes_history + logs/agent.log [YYYYMMDD_HHMMSS_xxx] session_id;session_search FTS5 因 WAL 未 checkpoint 常假阴,不可独信。
§
1+N 数字员工集成(2026-07-08):fork 到 Hermes。Skill `~/AppData/Local/hermes/skills/productivity/oneplusn/`,9 个 bash 命令在 `~/AppData/Local/hermes/bin/oneplusn*`(cp 副本,Windows ln-sf 坏),cron 30 分钟 + reaper 1 小时(hermes cron --no-agent)。Eval 10/10 PASS。配套 skill `claude-package-to-hermes-skill` 在 `devops/`,记 .claude 包移植方法论 + Windows 路径陷阱 + bash 包装 + cron + evals 模板。
§
Configured API keys: only MINIMAX_CN_API_KEY. Missing: OpenRouter, OpenAI, Google, xAI, Exa, Tavily, Firecrawl, Anthropic, GitHub, FAL, ElevenLabs. OAuth providers all unconfigured (Nous/Codex/xAI/Gemini). No GITHUB_TOKEN → 60 req/hr rate limit.
§
Toolset state (updated 2026-06-03): web (Tavily) ✓ enabled, browser (agent-browser + Playwright Chromium) ✓ enabled, browser-cdp still ⚠, image_gen/video_gen ⚠ (need FAL_KEY), x_search ⚠ (need XAI key), moa ⚠ (need OPENROUTER key), computer_use enabled but macOS-only (won't work on this Windows host). All other toolset gaps are by design — require their own platform credentials.
§
4-profile Agent Team (pm/eng/qa/ast) at ~/AppData/Local/hermes/profiles/,Kanban 60s tick 在 gateway(kanban.dispatch_in_gateway=true),manual ~/AppData/Local/hermes/USAGE.md。Pitfall:eng 用 kanban_block(review-required) 卡住 parent-linked QA,PM 手动解或修 eng 的 SOUL.md。E2E 验证 2026-06-03 mdlinkcheck CLI(37min,54/54 tests,99%)。
§
用户口头报告 ≠ 实际:用户说"好了/做完了"先用工具实测(gh auth status / curl / process list),别信口头。1+N 部署踩过。
§
Memory-cleanup cron(2026-07-13 21:00 首次运行):sessions=52,messages=1518,**0 条 >30 天**,无归档目标。MEMORY.md 7 条目均 < 30 天。hindsight 0.8.4 已装但 `~/.hermes/hindsight/config.json` 用 `llm_provider: openai_compatible`,daemon valid_providers 列表(llm_wrapper.py:616)不含该值,启动失败。manager profile 同病(0.8.4 daemon 启动时 HF 模型下载 asyncio 错误),boss/PM 需修:换成 `minimax` 或 `litellm`(但 `minimax` 默认 base_url 是 `api.minimax.io`,CN 端点 `api.minimaxi.com` 需手动覆盖)。今日不修全局配置,只清 daemon lock/log 残留。
§
Memory-cleanup cron(2026-07-14 21:00 第二次运行):MEMORY.md 8 条目 3109 字节,**0 条 >30 天**,无归档目标(各条目首次写入均在 2026-07-08~2026-07-13 区间,entry 5 body 内嵌的 2026-06-03 是 toolset 验证时戳,不是 entry 创建日,故仍 KEEP)。**hindsight 仍坏**(Known Fix #10):`~/AppData/Local/hermes/hindsight/config.json`(注:不在 `~/.hermes/hindsight/`,在 profile 父级)仍 `llm_provider: openai_compatible`,daemon 0.8.4 valid_providers(hindsight_api/engine/llm_wrapper.py:616)不含该值,启动 `Invalid LLM provider: openai_compatible` 失败。本 profile `~/.hindsight/profiles/` 下无 reviewer 日志(daemon 从未成功起),manager 日志末次 2026-07-12 21:54 仍是同一失败(boss/PM 未修)。**fix 路径**(boss 级,影响所有 profile):A — `llm_provider: litellm`(通用 openai_compatible 转发);B — `llm_provider: minimax` + `llm_base_url: https://api.minimaxi.com/v1`(CN 端点手动覆盖)。本 cron 不修全局配置(Known Fix #10 红线)。本 profile `memories/archive/` 尚未创建(无归档目标),不预创建。无 daemon lock/log 本地残留可清。pre-flight:gh api user 返回 handsomehu80(boss OAuth 回退,Known Fix #11)— employee .env 在本机但加载被覆盖,read-only 没事,write 会归到 boss 名下。memory-cleanup 只读,无影响。memory tool 本次 cron 仍未启用(provider=hindsight,daemon 启不来,retain/reflect 全静默 no-op),本轮只能直接 patch MEMORY.md。